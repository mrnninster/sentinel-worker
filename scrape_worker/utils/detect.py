"""Stub — full stream detect stack is not shipped in this package."""


class DetectUtils:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "utils.detect.DetectUtils is not available in wallfly_schedule_scraper. "
            "Use a different schedule_type or the LLM scrape mode."
        )
